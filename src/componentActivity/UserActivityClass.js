class UserActivityClass {
  constructor() {
    this.username = "";
  }

  setUsername(name) {
    this.username = name;
  }

  getUsername() {
    return this.username;
  }
}

export default UserActivityClass;
